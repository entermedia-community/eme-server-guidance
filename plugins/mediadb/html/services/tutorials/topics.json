{
      "response": {
            "status": "ok",
            "userid": "$user.id"
      },
      "topics": [
            #foreach($topic in $usertopics)
            #if($foreach.count > 1),#end
            {
                  "id": #jesc($topic.id),
                  "thumbnail": "$siteroot$mediaarchive.asLinkToPreview($topic.primarymedia, 'image200x200')",
                  "name": "#text($topic)",
                  "tutorials": #jesc($topic.tutorials),
                  "description": #jesc($topic.longcaption)
            }
            #end
      ]
}
